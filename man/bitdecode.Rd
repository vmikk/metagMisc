% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bitcode.R
\name{bitdecode}
\alias{bitdecode}
\title{Decode integer codes produced by \code{bitcode()}}
\usage{
bitdecode(
  code,
  levels = attr(code, "bitcode_levels"),
  bases = attr(code, "bitcode_bases"),
  order = attr(code, "bitcode_order"),
  start = attr(code, "bitcode_start"),
  varnames = attr(code, "bitcode_varnames"),
  is_factor = attr(code, "bitcode_is_factor"),
  na_code = NA
)
}
\arguments{
\item{code}{A vector produced by \code{bitcode()}, or a numeric/integer vector
plus explicit \code{levels}, \code{bases}, \code{order}, and \code{start}.}

\item{levels}{Optional list of level sets per variable. Defaults to
\code{attr(code, "bitcode_levels")}.}

\item{bases}{Optional integer vector of base sizes. Defaults to
\code{attr(code, "bitcode_bases")}.}

\item{order}{Encoding order, as in \code{bitcode()}. Defaults to
\code{attr(code, "bitcode_order")}.}

\item{start}{Start offset, as in \code{bitcode()}. Defaults to
\code{attr(code, "bitcode_start")}.}

\item{varnames}{Optional variable names. Defaults to
\code{attr(code, "bitcode_varnames")}.}

\item{is_factor}{Optional logical vector indicating which decoded variables
should be returned as factors (with the provided level sets as factor
levels). Defaults to \code{attr(code, "bitcode_is_factor")}.}

\item{na_code}{Value representing missing codes. Defaults to \code{NA}.}
}
\value{
A \code{data.frame} with one column per encoded variable.
}
\description{
Inverts \code{bitcode()} and reconstructs the original tuple of discrete
variables (as a data.frame) using the encoding metadata.
}
\examples{
code <- bitcode(c("A","B"), c("x","y"))
bitdecode(code)

}
